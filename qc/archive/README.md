# Archived QC scripts

These scripts are retained for provenance only. They are not called by the active QC utilities and still contain historical paths/assumptions. Prefer the scripts in the parent `qc/` directory for new runs.
